# AI 视觉对话助手 — 设计文档

> 七牛云 × XEngineer 暑期实训营 · 题目一
> 提交日期：2026-06-14 | 作者：mike

---

## 一、用户故事

### 计划实现

| # | 用户故事 | 优先级 | 验收标准 |
|---|---------|--------|---------|
| US-1 | 作为用户，我打开浏览器就能看到摄像头画面，无需安装任何软件 | P0 | 纯 HTML5，浏览器即开即用 |
| US-2 | 作为用户，我按住按钮说话、松开发送，像对讲机一样自然 | P0 | PTT 交互，按住录/松开发 |
| US-3 | 作为用户，AI 能根据我的语音问题，结合摄像头画面给出回答 | P0 | 视觉+语音多模态理解 |
| US-4 | 作为用户，AI 用语音回答我，不是打字 | P0 | TTS 流式语音合成 |
| US-5 | 作为用户，当我问纯文本问题时（不涉及画面），AI 也能回答 | P1 | 意图路由区分视觉/文本问题 |
| US-6 | 作为用户，对话有记忆，多轮聊天 AI 记得上文 | P1 | 三层语义压缩记忆 |
| US-7 | 作为用户，AI 服务出问题时我不会看到技术错误，而是友好降级提示 | P0 | 三级降级链，永不崩溃 |
| US-8 | 作为用户，我能看到实时的音量反馈，知道麦克风在工作 | P2 | VAD 音量回调驱动 UI 音量条 |
| US-9 | 作为用户，网络断了重连后还能继续用 | P1 | WS 指数退避重连，最多 10 次 |
| US-10 | 作为用户，我有一个专属的像素宠物陪伴我对话 | P2 | 4 种随机宠物 + 状态动画 |
| US-11 | 作为用户，我登录后下次打开网页自动记住我 | P2 | localStorage + SQLite 持久化 |
| US-12 | 作为用户，我可以切换账号换不同宠物 | P3 | 切换账号按钮 |
| US-13 | 作为用户，在手机上打开也能正常使用 | P2 | 响应式布局，触摸事件支持 |
| US-14 | 作为用户，我能看到和 AI 的对话历史气泡 | P3 | 聊天气泡 UI |

### 最终实现

| # | 用户故事 | 状态 | 实现要点 |
|---|---------|------|---------|
| US-1 | 浏览器即开即用 | ✅ 已实现 | `client/index.html`，getUserMedia API |
| US-2 | PTT 对讲机交互 | ✅ 已实现 | `client/vad.js` RMS 能量 VAD + 环形缓冲 |
| US-3 | 视觉多模态理解 | ✅ 已实现 | `server/vlm.py` Qwen-VL-Max，Canvas 512×512 截图 |
| US-4 | TTS 语音回答 | ✅ 已实现 | `server/tts.py` CosyVoice v1 流式，longxiaochun 音色 |
| US-5 | 意图路由 | ✅ 已实现 | `server/router.py` L0 22 条中文正则，零延迟 |
| US-6 | 对话记忆 | ✅ 已实现 | `server/memory.py` 三层压缩（短→中→背景） |
| US-7 | 降级容错 | ✅ 已实现 | `server/main.py` VLM→LLM→预设文案，每层独立 catch |
| US-8 | 音量反馈 | ✅ 已实现 | `client/vad.js` 每 5 帧回调 `onVolume(rms)` |
| US-9 | 断线重连 | ✅ 已实现 | `client/websocket.js` 指数退避 500ms→10s |
| US-10 | 像素宠物 | ✅ 已实现 | `client/pet.js` 4 种纯 CSS 宠物，4 状态动画 |
| US-11 | 自动登录 | ✅ 已实现 | `server/db.py` SQLite WAL，localStorage 持久化 |
| US-12 | 切换账号 | ✅ 已实现 | `switchAccount()` 清除状态 + 回到登录界面 |
| US-13 | 移动端适配 | ✅ 已实现 | `@media (max-width: 840px)` 响应式布局 |
| US-14 | 对话气泡 | ✅ 已实现 | 聊天气泡 UI，用户/AI 左右分布 |

**计划 14 条，实现 14 条，完成率 100%。**

### 未规划但实际新增

- **输入尺寸限制**（P0 致命伤修复）：音频 ≤512KB，图片 ≤256KB，base64 解码前拦截
- **pre-commit 密钥扫描**：`detect-dashscope-key` 正则拦截 `sk-[a-zA-Z0-9]{16,}` 模式
- **代理自动剥离**：`config.py` 导入时清除 `HTTP_PROXY`/`HTTPS_PROXY`，httpx `trust_env=False` 双重保险
- **Windows 兼容**：SelectorEventLoop 策略 + 临时目录 WAV 调试保存
- **CI 分层**：unit job（每次 push 跑 mock 测试）+ live job（仅 PR，需 API Key secret）
- **SQLite 并发安全**：WAL 模式 + aiosqlite 异步访问

---

## 二、运营成本控制

### 考虑到的技巧

| # | 技巧 | 预期效果 | 是否采用 | 说明 |
|---|------|---------|---------|------|
| C-1 | **L0 正则意图路由** | 省去每次对话的 LLM 分类调用（~1.7s + token 消耗） | ✅ 已采用 | 22 条中文正则覆盖视觉关键词，零延迟零成本 |
| C-2 | **图像压缩后上传** | 减少 VLM 输入 token（大图按像素计费） | ✅ 已采用 | Canvas 缩放至 512×512，JPEG q=0.7，Base64 体积降约 70% |
| C-3 | **客户端 VAD 削峰** | 只发送有效语音段，不传静音 | ✅ 已采用 | RMS 能量阈值 0.008 + 底噪估计 + 最短语音过滤 <0.4s |
| C-4 | **三级降级链** | VLM 失败不重试，降级 LLM 或预设文案，避免重试费叠加 | ✅ 已采用 | VLM→LLM→`FALLBACK_PRESET`，每层独立 catch |
| C-5 | **记忆压缩** | 长对话压缩上下文窗口，节省每轮 input token | ✅ 已采用 | 三层语义压缩：short(3轮)→mid(7条)→bg(≤150字) |
| C-6 | **TTS 流式合成** | 边生成边播放，降低首字延迟，不额外增加成本 | ✅ 已采用 | CosyVoice 流式 API，asyncio.Queue 桥接 |
| C-7 | **ASR 批量调用而非流式** | fun-asr-realtime 按调用次数计费，批量模式无额外成本 | ✅ 已采用 | `Recognition.call()` 同步调用，前 10 条本地保存 WAV 做 debug |
| C-8 | **无冗余 LLM 调用** | 意图分类不用 LLM，文本问题不走 VLM | ✅ 已采用 | L0 路由直接分流，文本问题跳过 VLM 的视觉推理 |
| C-9 | **输入大小硬限制** | 防止恶意/异常大消息耗尽 API 配额 | ✅ 已采用 | 512KB(音频)/256KB(图片)，解析前拦截 |
| C-10 | **CDN 缓存静态资源** | 减少前端资源的服务端带宽消耗 | ⬜ 未采用 | 当前为单体应用，后续可加 |
| C-11 | **请求频率限制** | 防止单个用户高频调用耗尽配额 | ⬜ 未采用 | 当前单用户场景，生产环境应用 FastAPI rate-limit 中间件 |
| C-12 | **音频格式选型** | 更低码率的 TTS 输出节省带宽 | ✅ 已采用 | MP3 16kHz mono 128kbps——语音场景足够清晰，比 320kbps 省 60% 带宽 |
| C-13 | **会话 TTL 过期清理** | 长时间不活跃的连接释放内存 | ⬜ 未采用 | 当前内存占用低，后续可加 `asyncio.timeout` 空闲断开 |
| C-14 | **缓存常见问题答案** | 重复问题（如"你叫什么"）不重复调 LLM | ⬜ 未采用 | 可能影响对话自然度，需权衡缓存粒度 |

### 成本估算模型

以单次视觉对话为例：

```
用户语音 5s (PCM 16kHz) → ASR → 文本 ~20 tokens
L0 正则路由 → 0 token, 0ms
VLM 推理 (512×512 JPEG) → ~200 input tokens + ~50 output tokens
LLM 兜底（仅 VLM 失败时）→ ~500 input + ~100 output tokens
TTS 合成 → ~300 chars → ~60s 音频

正常路径（VLM 成功）：
  ASR: 1 call + VLM: ~250 tokens + TTS: ~300 chars
  估算：ASR ¥0.003 + VLM ¥0.015 + TTS ¥0.006 ≈ ¥0.024/轮

降级路径（VLM 失败 → LLM）：
  ASR: 1 call + VLM: ~250 tokens（失败） + LLM: ~600 tokens + TTS: ~300 chars
  估算：¥0.003 + ¥0.015 + ¥0.003 + ¥0.006 ≈ ¥0.027/轮

纯文本问题（L0 路由 + LLM + TTS）：
  ASR + LLM + TTS ≈ ¥0.012/轮（省去 VLM，便宜 50%）
```

**核心结论：** L0 路由 + 三级降级 + 图像压缩三条线共同作用，将单轮成本控制在 ¥0.01–0.03，且在任何 AI 服务故障时都有兜底路径，不会因重试产生叠加费用。

---

## 三、端云协同架构

```
┌─ 浏览器（端）─────────────────────┐  ┌─ 服务端（云）──────────────────┐
│                                     │  │                               │
│  vad.js: RMS VAD + 5s 环形缓冲     │  │  asr.py: 通义听悟 ASR          │
│  camera.js: Canvas 512×512 JPEG     │  │  router.py: L0 正则意图路由    │
│  websocket.js: PCM 编码 + 重连      │  │  ├─ visual → vlm.py Qwen-VL-Max│
│  pet.js: 像素宠物 + 状态动画        │  │  └─ textual → llm.py DeepSeek  │
│                                     │  │  tts.py: CosyVoice 流式 TTS    │
│  端侧承担：VAD / 截图 / 编码       │  │  memory.py: 三层语义压缩       │
│  / UI 动画 / 重连逻辑              │  │  db.py: SQLite 用户-宠物       │
│                                     │  │                               │
│  端侧成本：0（用户设备）            │  │  云侧成本：API 调用 + 服务器   │
└─────────────────────────────────────┘  └───────────────────────────────┘
```

**端云划分原则：**
- **端侧做预处理，云侧做推理。** VAD 削峰、图像压缩、PCM 编码在浏览器完成，减少无效数据传输和云端计算。
- **不可替代的计算放云端。** ASR/VLM/LLM/TTS 四大 AI 引擎依赖专有模型，必须在云端调用。
- **端侧做容错，云侧做降级。** 断线重连由端侧指数退避驱动，服务降级由云侧三级降级链保证。

---

## 四、关键技术决策

| 决策 | 选项 A | 选项 B | 选择 | 理由 |
|------|--------|--------|------|------|
| 意图分类 | LLM 二分类（L1） | 纯正则（L0） | L0 正则 | ~1.7s 延迟在实时语音场景不可接受；正则 22 pattern 覆盖率足够 |
| ASR 模式 | 实时流式 | 批量调用 | 批量 call() | 用户已完整说完一句话，流式增量识别无实际收益 |
| 记忆方案 | 全量对话历史 | 三层压缩 | 三层压缩 | 长对话场景下 input token 线性增长不可控 |
| 图像大小 | 原图上传 | Canvas 512×512 | 512×512 | VLM 按像素计费，512×512 在识别精度和成本间平衡 |
| TTS 格式 | MP3 320kbps | MP3 16kHz mono 128kbps | 128kbps mono | 语音场景无需立体声/高码率，带宽省 60% |
| 数据库 | 无持久化 | SQLite WAL | SQLite WAL | 宠物映射需持久化，WAL 支持并发读写，零运维 |
| 宠物渲染 | 图片/SVG | 纯 CSS 像素画 | 纯 CSS | 零额外请求，4 种宠物共享同一 HTML 结构 |
