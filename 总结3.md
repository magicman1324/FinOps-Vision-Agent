# 总结3：AI 视觉对话助手架构评审 — 5 条元规则深度评估

> 上次更新: 2026-06-14 | 第 4 次评审 — 多人登录+像素宠物模块上线后

## 背景

第三次 XEngineer 项目——AI 视觉对话助手，FastAPI + WebSocket + HTML5 端云协同实时视觉对话应用。108 个 mock 测试 + 6 个真实 API 测试，4 个 AI 服务全覆盖。

对照 总结X.md 的 5 条元规则逐项评估。

---

## 元规则 1：对外契约是二进制判断 — 9/10

| 检查项 | 状态 | 证据 |
|--------|------|------|
| 依赖可装 | ✅ | `pip install -r server/requirements.txt` |
| 测试可过 | ✅ | `pytest tests/ -q --ignore=tests/test_live.py` → 108 passed |
| 服务可启动 | ✅ | `uvicorn server.main:app --host 127.0.0.1 --port 8765` |
| `.env` 不入仓库 | ✅ | `.gitignore` 包含 `.env`，`git log --all -- .env` 返回空 |
| 无硬编码密钥 | ✅ | 全部走 `os.getenv()` |
| **pre-commit 自动扫描密钥** | ✅ | `detect-dashscope-key` 正则 `sk-[a-zA-Z0-9]{16,}` |

**扣 1 分：** 缺 `truffleHog` 或 `git-secrets` 级的历史深度扫描。pre-commit 只拦新提交，不防已存在于历史中的密钥。

**对比前两次：** 第一次密钥写源码，第二次 `.env` 提交到 Git。这次 `.env` 在 `.gitignore` 里且加了 pre-commit 自动扫描。**第一条元规则首次被真正满足。**

---

## 元规则 2：数据流完整性 > 代码量 — 7/10（原 5/10）

### 主路径 ✅

```
audio → ASR → L0路由 → cascade_visual/cascade_text → TTS → WebSocket push
image → VLM → vlm_result
```

每一步的输出是下一步的输入，无断裂。

### 已修复 ✅

| # | 问题 | 修复 | PR |
|---|------|------|-----|
| 1 | L1 LLM 路由从未接入 | **移除 L1**。L0 正则（22 patterns）为唯一意图分类器。L1 的 ~1.7s LLM 往返在实时语音场景下是负优化。 | #32 + #36 |
| 2 | 记忆压缩从不触发 | **已接入** `main.py:132-135`。`compress_mid` 在 mid 溢出时触发，`compress_background` 在 mid≥2 且已压缩后触发。 | #33 + #36 |

### 修复中引入的新问题 ⚠️

记忆压缩的触发条件有微妙时序：`compress_background` 只在 `mid_compressed=True` 时触发，但 `compress_mid` 是异步的——同一次 `_handle_audio` 调用中 `compress_mid` 刚被 `create_task` 启动，下一行检查 `mid_compressed` 时它还没完成。这意味着 `compress_background` 只在"后一轮对话 + compress_mid 已完成"的条件窗口触发。**低概率，高轮次对话中可自愈。**

### 仍然存在的问题

- `classify_intent` 别名（`router.py:70`）零调用，死代码未删（P2，2 行改动）
- 5 个 config 常量已删除，但 `ASR_TIMEOUT` 仍未被 asr.py 使用（只是导入）

---

## 元规则 3：类型安全的边界原则 — 4/10（未变）

### 做得好的 ✅

大部分函数有返回类型标注：
```python
def speech_to_text(audio_base64: str) -> str:
async def ask_llm(prompt: str, system_prompt: str = None) -> str:
```

### 边界薄弱 ❌

系统入口——WebSocket JSON 消息处理——用了 `dict`（Python 版 `any`），TypedDict 已定义但编译期无约束：

```python
# main.py:92,147 — TypedDict 定义了但 json.loads() 返回普通 dict
async def _handle_audio(ws: WebSocket, data: AudioMessage):  # AudioMessage 只是文档
async def _handle_visual(ws: WebSocket, data: ImageMessage):  # 运行时零校验
```

**修复方案：** 用 Pydantic 或 dataclass 在 `json.loads` 之后做运行时验证。改动量 ~20 行，风险低。

---

## 元规则 4：工具是手段，约束才是目标 — 7/10（原 6/10）

| 工具 | 约束 | 验证方法 | 状态 |
|------|------|----------|------|
| `.env` + pre-commit | 密钥不进入 Git | `git log --all -- .env` 空 | ✅ |
| pytest | `pytest` 验证功能正确 | 108 测试全绿 | ✅ |
| 模块拆分 | 每个模块有调用方 | 导入链路检查 | ⚠️ L1 已移除，`classify_intent` 别名死代码仍存 |
| 日志 | 请求可追踪 | 同请求日志有相同 trace ID | ⚠️ trace ID 生成但不下传到 ASR/VLM/LLM/TTS |
| conftest mock | 测试不调真实 API | 同时 patch `server.main` + `server.llm` | ✅ 已修复 |

**已修复：** 代理清除逻辑从三处重复合并到 `config.py` 一处。

**仍未修复：** trace ID 下传。`main.py:65` 生成 `trace = str(uuid.uuid4())[:8]`，传入 `_handle_audio`，但不下传到底层服务。`llm.py`、`vlm.py`、`asr.py`、`tts.py` 的日志全无 trace context。多连接并发时无法关联请求。**改动量 ~30 行，零风险。**

---

## 元规则 5：Demo 堆功能，产品修致命伤 — 8/10（原 7/10）

### 产品思维体现 ✅

- 三级降级链：VLM→LLM→预设文案，每层独立 catch
- WS 重连加固：最大 10 次重试，指数退避，超限通知 UI
- CI 分层：unit（每次 push）+ live（仅 PR）
- L1 移除：为实时语音延迟砍掉 ~1.7s LLM 往返

### P0 致命伤修复（PR #36）✅

| # | 问题 | 严重度 | 修复 |
|---|------|--------|------|
| 1 | `speech_to_text` 同步阻塞事件循环 | Critical | `asyncio.to_thread` 包装 |
| 2 | VLM 无超时保护 | Critical | `asyncio.wait_for(timeout=VLM_TIMEOUT)` |
| 3 | Blob URL 永不释放 | Critical | `URL.revokeObjectURL()` 在每次 `_playNext` |
| 4 | play() 拒绝 → TTS 队列永久卡死 | Critical | `.catch()` 中恢复播放 |
| 5 | 无 touchcancel 处理 | Critical | 添加 `touchcancel` 事件 |
| 6 | conftest mock 目标遗漏 | Critical | 双 patch `server.main` + `server.llm` |

### P1 防御加固（PR #36）✅

| # | 问题 | 修复 |
|---|------|------|
| 7 | `_mid` 竞态条件 | `asyncio.Lock` + 快照迭代 |
| 8 | `asyncio.create_task` 吞异常 | `add_done_callback` 错误日志 |
| 9 | cascade WebSocket 测试假阳性 | 加 `text_result` 内容断言 |
| 10 | AudioContext 泄漏 | `startVAD` 前调用 `stopVAD` |
| 11 | `sendImage` 返回值忽略 | 检查返回值 + console.warn |
| 12 | `_mid_compressed` 全失败也设 True | `any_compressed` flag |
| 13 | 5 个 config 常量未消费 | 删除 |
| 14 | 代理清除三处重复 | 合并到 `config.py` |

---

## 总评

| 维度 | 第一次 | 第二次 | 第三次（修复前） | 第三次（修复后） |
|------|--------|--------|-----------------|-----------------|
| 密钥管理 | 源码硬编码 | `.env` 进 Git | ✅ `.gitignore` | ✅ + pre-commit |
| 构建系统 | 无 | ES Modules + TS | ✅ pip + uvicorn | ✅ |
| 测试 | `echo "no test"` | 17 文件只跑 1 | ✅ 108 mock | ✅ + mock 加固 |
| 类型系统 | 纯 JS | TS + 6 处 any | ⚠️ 有标注边界用 dict | ⚠️ 未变 |
| 可观测性 | `console.log` | Logger 无 trace | ⚠️ trace ID 有生成无传播 | ⚠️ 未变 |
| 数据流 | 上帝方法 | 管道有 3 处断裂 | ⚠️ 2 处断裂 | ✅ L1 移除、压缩接入 |
| 致命伤 | 多处 | 多处 | - | ✅ P0×6 + P1×8 已修 |

**趋势持续向上。** 密钥管理从两次失败到这次加上 pre-commit 自动扫描。从"语法层学习"进步到"约束层"——知道 `.env` 是工具，密钥不入 Git 是约束，pre-commit hook 是自动化强制执行。

但 trace ID 传播和 TypedDict→Pydantic 两个 P2 项未修。改动量小（合计 ~50 行），下次迭代可顺手修掉。

---

## 修复状态

| # | 问题 | 状态 | PR |
|---|------|------|-----|
| 1 | L1 路由 | ✅ 已移除 | #32 |
| 2 | 记忆压缩触发 | ✅ 已接入 | #33 |
| 3 | WebSocket TypedDict | ⬜ P2 | — |
| 4 | trace ID 下传 | ⬜ P2 | — |
| 5 | `classify_intent` 死代码 | ⬜ P2 | — |
| 6-19 | P0×6 + P1×8 | ✅ 已修 | #36 |

---

## 第 4 次评审 — 多人登录 + 像素宠物模块（2026-06-14）

PR #37 新增：4 种 CSS 像素宠物（机器人/猫/狗/外星人）、用户名登录、SQLite 持久化、localStorage 自动登录、切换账号。

### 按 5 条元规则逐项评估

#### 元规则 1：对外契约 — 8/10

| 检查项 | 状态 | 问题 |
|--------|------|------|
| 依赖可装 | ✅ | `aiosqlite` 新增 |
| 测试可过 | ✅ | 108 passed |
| 服务可启动 | ✅ | lifespan 初始化 DB |
| `*.db` 不入仓库 | ✅ | `.gitignore` |
| **`config.py` 环境变量健壮性** | ❌ | `int(os.getenv(...))` 无 try/except — 非法值直接崩溃 |
| **`asr.py` import 副作用** | ❌ | `os.makedirs` 在模块级执行 — 无写权限时炸 import |

#### 元规则 2：数据流完整性 — 5/10

| 断裂点 | 描述 |
|--------|------|
| `router.py:55-67` `classify_intent_l1` | 死代码（总结3 标 P2 未修） |
| `tts.py:84` `except Exception: raise` | no-op 异常处理器 |
| `main.py:47-58` `/login` 返回 `pet_type` | 服务端零消费，纯透传 |
| `main.py:24-31` `AudioMessage`/`ImageMessage` | TypedDict 仅文档用途，无运行时校验 |

#### 元规则 3：类型安全边界 — 3/10（比上次更低）

- **`db.py:get_or_create_user` 返回 `dict`** — 数据库查询结果是系统新边界，裸 dict 导致 key 拼写错误编译期零提示
- WebSocket 消息仍是 `dict`（TypedDict 仅文档，不校验）
- `/login` 请求用了 Pydantic（对的方向），返回仍是裸 dict

#### 元规则 4：工具是手段，约束才是目标 — 5/10

| 工具 | 约束 | 验证 | 状态 |
|------|------|------|------|
| SQLite + aiosqlite | 用户-宠物持久化 | 两次登录同用户同宠物 | ✅ |
| localStorage | 记住登录状态 | 刷新无需重输 | ✅ |
| **SQLite 无 WAL** | 并发写安全 | 多连接同时 login → 锁竞争 | ❌ |
| **`id(ws)` 作 dict key** | 连接隔离 | Python `id()` 回收 → 碰撞风险 | ❌ |
| **`asyncio.wait_for(to_thread)`** | VLM 超时 | 超时杀不掉线程 → 线程泄漏 | ❌ |
| **`_debug_save_count` 无锁** | ASR 计数器 | 并发连接计数错乱 | ⚠️ |

**新增 4 个工具，3 个约束未验证。** 每次加功能时"这个工具要保证什么约束"的追问没跟上。

#### 元规则 5：Demo 堆功能，产品修致命伤 — 6/10

**P0 致命伤（本次发现）：**

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| 1 | `id(ws)` 连接标识可能碰撞 | `main.py:67-68` | Critical |
| 2 | 无输入大小限制，内存可被耗尽 | `main.py:123,181` | Critical |
| 3 | `config.py` 非法值 crash 进程 | `config.py:27-30` | Critical |
| 4 | `pttBtn.innerHTML` — XSS 模式 | `index.html:719,724` | High |

**P1 防御加固（本次发现/修复）：**

| # | 问题 | 位置 |
|---|------|------|
| 5 | `classify_intent_l1` 死代码 | `router.py:55-67` |
| 6 | `except Exception: raise` no-op | `tts.py:84` |
| 7 | `os.makedirs` import 时执行 | `asr.py:23` |
| 8 | SQLite 无 WAL 模式 | `db.py` |
| 9 | 登录/宠物模块零测试 | 全模块 |
| 10 | `switchAccount` 不断开 WS/VAD | `index.html:667` |

### 总评（跨 4 次对比）

| 维度 | 第三次（修复后） | 第四次（本次） | 趋势 |
|------|-----------------|---------------|------|
| 密钥管理 | ✅ + pre-commit | ✅ | → |
| 构建系统 | ✅ pip + uvicorn | ✅ | → |
| 测试 | ✅ 108 mock | ✅ 108（新功能补了 5 条） | ✅ |
| 类型系统 | ⚠️ TypedDict 仅文档 | ❌ 新增 `dict` 返回类型 | ↓ |
| 可观测性 | ⚠️ trace ID 不传播 | ⚠️ 未变 | → |
| 数据流 | ✅ L1 移除、压缩接入 | ⚠️ `classify_intent_l1` 死代码仍未删 | ↓ |
| 致命伤 | ✅ P0×6 + P1×8 | ⚠️ 新引入 `id(ws)` 键冲突 + 无输入限制 | ↓ |

**核心问题未变：加功能的速度超过了修致命伤的速度。** `classify_intent_l1` 在总结3 标记 P2 说"下次迭代顺手修掉"，这次迭代加了登录+宠物但没有删死代码。这正是总结X.md 诊断的模式重复——每次学到工具语法，但没学到"约束必须验证"。

### 本次修复（PR #38 / 即将提交）

| # | 问题 | 修复 |
|---|------|------|
| P0-1 | `id(ws)` → uuid 连接标识 | `ws._x3_cid = str(uuid.uuid4())[:12]` |
| P0-2 | 输入大小限制 | `MAX_AUDIO_B64_BYTES=512KB`, `MAX_IMAGE_B64_BYTES=256KB` |
| P0-3 | `config.py` `int()` 崩溃 | `_int_env()` 封装 try/except + warning |
| P1-1 | `asr.py` import 时 `os.makedirs` | 移到 `speech_to_text` 函数内懒创建 |
| P1-2 | `classify_intent_l1` 死代码 | 删除函数 + `L1_SYSTEM_PROMPT` + 移除 import |
| P1-3 | `tts.py` no-op handler | 删除 `except Exception: raise` |
| P1-4 | `db.py` 无 WAL 模式 | `PRAGMA journal_mode=WAL` |
| P1-5 | 登录模块零测试 | 新增 `test_login.py` 5 条测试 |
| P2-1 | `switchAccount` 不断开连接 | 调 `disconnect()` + `stopVAD()` + 清队列 |
| P2-2 | `pttBtn.innerHTML` XSS 模式 | 改为 `ptt-text` span + `textContent` |
| P2-3 | `在哪[儿里]?` 正则假阳性 | 保持 `?`（召回优先，精度由"无图片"兜底保证） |

### 仍需修复（下次迭代）

| # | 问题 | 改动量 |
|---|------|--------|
| TypedDict→Pydantic | WebSocket 消息运行时校验 | ~20 行 |
| trace ID 下传 | 传到 ASR/VLM/LLM/TTS 的日志 | ~30 行 |
| `get_or_create_user` 返回类型 | Pydantic model 替代 dict | ~5 行 |
| `responses_model` | `/login` 加 response_model | ~3 行 |
