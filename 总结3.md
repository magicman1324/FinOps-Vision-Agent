# 总结3：AI 视觉对话助手架构评审 — 5 条元规则深度评估

## 背景

这是第三次 XEngineer 项目——AI 视觉对话助手，FastAPI + WebSocket + HTML5 端云协同实时视觉对话应用。31 个 PR，106 个 mock 测试 + 6 个真实 API 测试，4 个 AI 服务全覆盖。

对照 总结X.md 的 5 条元规则逐项评估。

---

## 元规则 1：对外契约是二进制判断 — 9/10

| 检查项 | 状态 | 证据 |
|--------|------|------|
| 依赖可装 | ✅ | `pip install -r server/requirements.txt` |
| 测试可过 | ✅ | `pytest tests/ -q --ignore=tests/test_live.py` → 106 passed |
| 服务可启动 | ✅ | `uvicorn server.main:app --host 127.0.0.1 --port 8765` |
| `.env` 不入仓库 | ✅ | `.gitignore` 包含 `.env`，`git log --all -- .env` 返回空 |
| 无硬编码密钥 | ✅ | 全部走 `os.getenv()` |

**扣 1 分：** 缺 pre-commit hook 自动扫描密钥。如果哪天 `.gitignore` 被误删，没有最后一道防线。

**对比前两次：** 这是你第一次把这条规则做对。第一次密钥写源码，第二次 `.env` 提交到 Git。这次 `.env` 在 `.gitignore` 里且从未出现在 Git 历史中。进步是根本性的。

---

## 元规则 2：数据流完整性 > 代码量 — 5/10

### 主路径 ✅

```
audio → ASR → L0路由 → cascade_visual/cascade_text → TTS → WebSocket push
image → VLM → vlm_result
```

每一步的输出是下一步的输入，无断裂。

### 断裂 #1：L1 LLM 路由未接入 ❌

```
router.py:55  classify_intent_l1  ← 5 个测试通过
main.py:96    classify_intent_l0  ← 只调了 L0
              classify_intent_l1  ← 从未调用
```

L0 正则命中 ~85% 场景，剩余 15% 的边缘视觉意图永远落在 L0 的 `"textual"` 默认值，走 LLM 文本路径而非 VLM 视觉路径。

**与 X-Reviewer RateLimiter 同构：** 写了，测了，没连线。

### 断裂 #2：记忆压缩从不触发 ❌

```
memory.py:61  compress_mid()        ← 7 个测试通过
memory.py:79  compress_background() ← 7 个测试通过
main.py                               ← 从未调用
```

对话开始后 short→mid evict 正常，但 mid 永不被压缩。超过 10 轮后 mid overflow 直接丢弃最旧条目，信息永久丢失。三层压缩退化成了 FIFO 队列。

### 死代码：classify_intent 别名 ❌

```
router.py:70  classify_intent()  ← 向后兼容别名
              代码库中零调用方
```

---

## 元规则 3：类型安全的边界原则 — 4/10

### 做得好的 ✅

大部分函数有返回类型标注：
```python
def speech_to_text(audio_base64: str) -> str:
async def ask_llm(prompt: str, system_prompt: str = None) -> str:
```

### 边界薄弱 ❌

系统入口——WebSocket JSON 消息处理函数——用了 `dict`（Python 版 `any`）：

```python
# main.py:75,121
async def _handle_audio(ws: WebSocket, data: dict):
async def _handle_visual(ws: WebSocket, data: dict):
```

JSON payload 来自网络，结构不可控。系统边界正是类型系统产生最大价值的地方。应该用 `TypedDict`：

```python
class AudioMessage(TypedDict):
    type: Literal["audio"]
    audio: str
```

---

## 元规则 4：工具是手段，约束才是目标 — 6/10

| 工具 | 约束 | 验证方法 | 状态 |
|------|------|----------|------|
| `.env` | 密钥不进入 Git | `git log --all -- .env` 空 | ✅ |
| pytest | `pytest` 验证功能正确 | 106 测试全绿 | ✅ |
| 模块拆分 | 每个模块有调用方 | 导入链路检查 | ⚠️ L1/压缩 无调用 |
| 日志 | 请求可追踪 | 同请求日志有相同 trace ID | ❌ 无 trace ID |

日志格式 `%(asctime)s [%(levelname)s] %(message)s`，多连接并发时无法关联同一条请求的日志。

---

## 元规则 5：Demo 堆功能，产品修致命伤 — 7/10

### 产品思维体现 ✅

- 三级降级链：VLM→LLM→预设文案，每层独立 catch
- WS 重连加固：最大 10 次重试，指数退避，超限通知 UI
- CI 分层：unit（每次 push）+ live（仅 PR）

### 致命伤 ❌

L1 路由断线和记忆压缩不触发——两个设计意图没有兑现，代码存在但不产生价值。评委看到 `classify_intent_l1` 存在但 `main.py` 不调，结论就是"架构有设计但实现不完整"——和 X-Reviewer 的评语会是一样的。

---

## 总评

| 维度 | 第一次 | 第二次 | 第三次 |
|------|--------|--------|--------|
| 密钥管理 | 源码硬编码 | `.env` 进 Git | ✅ `.gitignore` 排除 |
| 构建系统 | 无 | ES Modules + TS | ✅ pip + uvicorn |
| 测试 | `echo "no test"` | 17 文件只跑 1 | ✅ 106 mock + 6 live |
| 类型系统 | 纯 JS | TS + 6 处 `any` | ⚠️ 有标注但边界用 `dict` |
| 可观测性 | `console.log` | Logger 无 trace | ⚠️ 无 trace ID |
| 数据流 | 上帝方法 | 管道有 3 处断裂 | ⚠️ 2 处断裂 |

**趋势是向上的，且加速明显。** 密钥管理从两次失败到这次彻底解决。测试从 `echo "no test"` 到 112 个测试覆盖全链路。

但"写了没连线"这个模式第三次出现了。L1 路由和记忆压缩——和 X-Reviewer 的 RateLimiter 一样——代码质量没问题，测试全绿，但数据流不完整。

**修复代价极低：** 三个改动都不超过两行代码。不是能力问题，是缺一个"交之前从入口到出口走一遍数据流"的肌肉记忆。

---

## 修复清单

按优先级排列：

| # | 问题 | 文件 | 改动量 | PR |
|---|------|------|--------|-----|
| 1 | L1 路由接入 main.py | `server/main.py:96` | 2 行 | #32 |
| 2 | 记忆压缩在管线中触发 | `server/main.py:111` | 3 行 | #33 |
| 3 | WebSocket 消息 TypedDict 强类型 | `server/main.py` | ~20 行 | #34 |
| 4 | 日志加 trace ID | `server/main.py:16,47` | 5 行 | #34 |
| 5 | 删除 classify_intent 死代码 | `server/router.py:70-72` | 3 行 | #34 |
