# AI 视觉对话助手 — 面试问答

---

## 项目架构类

**Q1: 介绍你的项目，用一句话说清楚它解决什么问题。**

AI 视觉对话助手——打开摄像头，AI 看到你的画面、听到你的声音，用语音回答你。核心价值是"端云协同的多模态实时对话"，让用户像跟人视频通话一样跟 AI 交互。

**Q2: 为什么选择 WebSocket 而不是 HTTP 轮询？**

实时语音对话场景要求全双工通信——用户说话的同时 AI 可能在推送 TTS 音频。HTTP 轮询延迟高、开销大。WebSocket 一次握手后双向推送，ASR 结果、TTS 音频流、错误降级通知全走一条连接。

**Q3: 你的三级降级链是怎么设计的？为什么不用重试？**

VLM → LLM → 预设文案。每层独立 try/catch，失败不重试而是降级。因为 AI API 失败通常是配额、网络或模型过载问题，重试大概率也失败，还会叠加费用。降级保证用户体验不中断——VLM 看不到画面时，LLM 会诚实告诉用户"我看不到画面，但根据常识回答..."，最差也有兜底文案。

Vision Language Model（视觉语言模型）——能同时理解图片和文字的 AI 模型。



具体做法分四步：

  1. 故障枚举，不是拍脑袋加 try/catch

  把 AI API 调用链上每一环可能的失败方式穷举出来：

  ┌──────┬────────────────────────────────────┬────────────────┐
  │ 环节 │             可能怎么挂             │      后果      │
  ├──────┼────────────────────────────────────┼────────────────┤
  │ VLM  │ 超时、配额耗尽、图片损坏、模型过载 │ 视觉问题没答案 │
  ├──────┼────────────────────────────────────┼────────────────┤
  │ LLM  │ 超时、API Key 失效、流式中断       │ 文本问题没答案 │
  ├──────┼────────────────────────────────────┼────────────────┤
  │ TTS  │ 合成失败、音频流中断               │ 有文字没声音   │
  └──────┴────────────────────────────────────┴────────────────┘

  每种故障对应一个降级动作，不是笼统地 "except Exception: pass"。

  2. 分层设计，每层只知道自己和下一层

  视觉问题: _cascade_visual
    L1: VLM（看图回答）
      ↓ VLMError
    L2: LLM（告知"我看不到画面，凭常识回答"）
      ↓ LLMError
    L3: FALLBACK_PRESET（"抱歉，我暂时无法处理"）

  文本问题: _cascade_text
    L1: LLM（正常回答）
      ↓ LLMError
    L2: FALLBACK_PRESET

  关键设计决策：降级不是静默的。 VLM 失败降到 LLM 时，prompt
  变了——不再是"请看图回答"，而是"你暂时看不到画面，请根据常识回答"。这让 LLM
  知道自己处于降级模式，输出会更诚实（"抱歉，我看不到画面，无法确定颜色"），而不是瞎编。

  3. 每层独立 catch，不跨层传染

L1

  try:
      return await image_to_text(image, prompt)
  except VLMError:
      logger.warning("VLM failed, cascading to LLM")

L2 — 完全独立的 try 块

  try:
      return await ask_llm(llm_prompt, ...)
  except LLMError:
      logger.warning("LLM also failed, using preset fallback")

L3 — 不需要 try，纯字符串

  return FALLBACK_PRESET

  VLM 抛异常不会跳到 catch LLM 的代码路径，LLM 抛异常也不会尝试重试 VLM。每层只关心自己：要么成功返回，要么异常交给下一层。  

  4. 不重试，直接降——这是成本决策

  VLM 超时 15 秒 → 不是 sleep 一下再试，而是立刻降到 LLM。因为：
  - AI API 失败原因 90% 是配额/网络/过载，重试大概率也失败
  - 重试会让用户多等 15-30 秒，语音场景体验不可接受
  - 重试 = 叠加费用（失败那次也计费）

  降级链的总响应时间 = max(单层超时, 下一层耗时)，不是各层之和。VLM 挂了的用户体验是：等 15 秒 → 立刻得到 LLM 回答。不会傻等 
  30 秒重试。

  核心 trade-off：可用性优先于完美。
  宁可告诉用户"我看不到画面，根据常识..."（降级体验），也不让用户看到技术错误或无限等待。最后还有 FALLBACK_PRESET
  兜底——不存在"什么都返回不了"的状态。

---

## 成本控制类（题目一重点）

**Q4: 你的 L0 正则路由省了多少钱？**

每次对话省一次 LLM 调用（~1.7s 延迟 + token 消耗）。22 条中文正则覆盖视觉关键词（"这是什么""什么颜色""在哪"等），零延迟零成本。之前有 L1 LLM 二分类兜底，但 ~1.7s 往返在实时语音场景不可接受，PR #32 移除了。按日活 100 用户、每人 10 轮对话算，每天省 1000 次 LLM 调用。

L0 正则路由就是你 server/router.py 里的 classify_intent_l0 函数——用 22条中文正则表达式判断用户说的话是不是"视觉问题"，零延迟、零成本。

  举例：用户说"这是什么颜色" → 命中 r"什么颜色" → 返回 "visual" → 调 VLM 看图回答。用户说"今天天气怎么样" → 22 条全部 miss → 返回 "textual" → 直接调 LLM，省掉一次 VLM 调用。

  之所以叫 L0（Level 0），是因为之前有个 L1（LLM 二分类）做兜底——L0 没命中时 L1 让 LLM 再判一次。但 ~1.7s
  延迟在实时语音场景太慢，PR #32 把 L1 移除了，L0 现在是唯一意图分类器。



![image-20260623205630568](C:\Users\magic\AppData\Roaming\Typora\typora-user-images\image-20260623205630568.png)

具体做法分四步：

  1. 场景枚举，不是语料训练

  不要语料库、不用
  ML。直接列出一个人打开摄像头后会说的所有自然语句——"这是什么""什么颜色""在哪""有几个""你看到了什么""我手里是什么"。30       
  分钟穷举出 ~50 句原始语料。

  2. 语义聚类，归并成正则

  把 50 句按语义分类成 10 组。同组内找共同模式转正则：

  - "这是什么""那是什么""什么东西" → r"这是什么" r"那是什么" r"什么东西"（三条独立，因为核心词不同）
  - "这个东西是什么""这个东西什么颜色" → r"这个(东西|物品|物件|颜色|形状|人|是啥|是什么)"（一条带分支，管道符枚举宾语）      
  - "有几个""多少个""多少张纸" → r"有几个" r"多少个" r"多少[个只件张把台辆朵棵粒座]"（字符类覆盖量词）

  关键技巧：在哪[儿里]? 而不是 在哪——北方说"哪儿"，南方说"哪里"，? 覆盖"你"字弱读。

  3. 测试验证，不做训练

  不是拿标注数据算 precision/recall，而是写 22 条 pytest 用例覆盖边界：

命中该命中的

  assert classify_intent_l0("这是什么颜色") == "visual"
  assert classify_intent_l0("钥匙在哪") == "visual"

不命中不该命中的

  assert classify_intent_l0("你好") == "textual"
  assert classify_intent_l0("今天天气怎么样") == "textual"

边界

  assert classify_intent_l0("") == "textual"
  assert classify_intent_l0("   ") == "textual"

  tests/test_router.py 覆盖了命中场景（9 个测试类）、未命中场景（1 个类）、边界场景（1 个类）。

  4. 线上观察，迭代砍 L1

  最初设计是 L0 正则 + L1 LLM 双层分类——L0 miss 时 LLM 兜底。上线后发现 L0 覆盖率足够高，L1 的 ~1.7s 延迟反而破坏实时体验。PR   #32 直接移除 L1，L0 成为唯一分类器。代码从 ~50 行减到 37 行，还快了 1.7 秒。

  核心 trade-off：召回优先于精确。 把"你看这样行吗"误判成 visual（假阳性）→ VLM 也能回答，用户无感。把"这是什么"漏判成       
  textual（假阴性）→ LLM 看不到画面，用户困惑。所以宁可宽进，靠 VLM/LLM 的智能兜底。



**Q5: 图像压缩怎么做的？为什么是 512×512？**

Canvas 缩放至 512×512，JPEG q=0.7。VLM 按像素计费，原图可能是 1920×1080=200 万像素，压缩后 26 万像素，体积降约 70%。512×512 是识别精度和成本的平衡点——再小会影响文字和细节识别。

**Q6: 你还考虑过哪些成本控制手段但没采用？为什么？**

设计文档列了 14 项，采用了 10 项。没采用的：CDN 缓存（单体应用不需要）、频率限制（单用户场景）、会话 TTL（当前内存占用低）、答案缓存（影响对话自然度）。每个决策都有场景适配的考量，不是越多越好。

---

## 工程实践类

**Q7: 108 个测试怎么组织的？为什么 mock 测试和真实 API 测试分开？**

`conftest.py` 有 autouse fixture 全局 mock `ask_llm`，所有测试自动启用，防止意外调真实 API 烧钱。mock 测试每次 push 都跑（CI unit job），真实 API 测试只在 PR 时跑（CI live job），需要 `DASHSCOPE_API_KEY` + `DEEPSEEK_API_KEY` secret。分层避免 CI 配额浪费。



mock测试是用假数据替代真实依赖的测试。

  本项目里，conftest.py 全局 patch 了 ask_llm 函数，把真实 DeepSeek 调用替换成直接返回 "mock response"。这样跑 108条测试不需要 API Key、不花钱、不耗配额，1.5 秒跑完。

  对比真实 API 测试：test_live.py 需要 DASHSCOPE_API_KEY + DEEPSEEK_API_KEY 环境变量，真的调云端模型，慢且花钱。所以 mock 测试每次 push 都跑（CI unit job），真实 API 测试只在 PR 时跑（CI live job）。

  核心思想：测试的是你自己的逻辑对不对，不是测 DeepSeek 的 API 能不能用。

**Q8: 你遇到了什么技术难点？怎么解决的？**

最大的坑是 CosyVoice TTS 的回调桥接——SDK 是同步回调 `on_data(bytes)`，但 FastAPI WebSocket 需要 `async for` 流式推送。用 `asyncio.Queue` 做桥接，回调 `put_nowait`，生成器 `await queue.get()`，解耦同步回调层和异步推送层。还有个坑是 `id(ws)` 做连接标识——Python 会回收 id，PR #38 改成了 `uuid.uuid4()[:12]` 存在 ws 对象属性上。

**Q9: pre-commit hook 你加了什么？**

`detect-dashscope-key`——正则 `sk-[a-zA-Z0-9]{16,}` 拦截 DashScope/DeepSeek API Key 格式。之前两次选拔都有密钥泄露问题（源码硬编码→.env 进 Git），这次是第一次真正守住。还加了 `detect-private-key` + `check-added-large-files`。

**Q9-b: 你遇到最大的坑是什么？怎么解决的？**

最大的坑是语音采集——一开始用自动 VAD 检测说话开始/结束，调试了一整天，永远丢开头。

**Phase 1：自动 VAD 设计**

原始方案：RMS 能量阈值 0.01，连续 3 帧高于阈值判定"开始说话"，静音 1.5s 判定"说完"。3 秒环形缓冲区，回溯 0.3s 保留开头。

问题立刻暴露：ASR 永远只收到末尾几个字，开头全丢。

**Phase 2：7 次修补，每次只修一个症状（git log 铁证）**

| 修复 | 尝试解决 | commit |
|------|---------|--------|
| 回溯 0.1→0.8s | VAD 检测延迟导致首字丢失 | `fa259b2` |
| 阈值 0.01→0.0005，2 帧触发 | 轻声辅音 s/sh/x/h/f 振幅太低不被检测 | `54f6241` |
| 动态归一化 20x 增益 | 安静环境说话音量不足 | `53bf3a6` |
| 相对阈值替换固定阈值 | "葡萄皮"的 p/t/p 清爆破音被切掉 | `cc10716` |
| 静音超时 0.6→2.0s | 换气被当成句尾，一句话拆成碎片 | `92f5280` |
| 最小语音 0.8s 过滤 | 噪声误触发 | `d96e30f` |
| 环形缓冲 3→5s | 长句超过缓冲，开头被覆盖 | `53bf3a6` |

参数互相打架。静音超时拉到 2 秒解决换气切段，但用户说完要等 2 秒。降到 0.6 秒响应快了，换气又切段。**这是无解的——无论怎么调参，自动 VAD 无法区分"换气"和"说完"。**

**Phase 3：推倒重来——从微信语音获得启发（`18cd61f`）**

核心洞察：微信语音消息的"按住说话"——**什么时候说完，用户自己知道。为什么让机器猜？**

把整个 VAD 拆掉，换成按键通话：

| 自动 VAD | 按键通话 |
|----------|---------|
| 静音阈值猜 end-of-speech | 用户松手 = 说完 |
| 换气被切断 | 按多久录多久 |
| 参数调不完 | 几乎零参数 |
| 回溯长度猜 start | 按键瞬间标记起始位 |

代码从 ~170 行自动检测逻辑删到 ~50 行：`startRecording()` 清缓冲+标记起始位，`stopRecording()` 裁剪静音+回调，中间持续写环形缓冲。

**教训：把 AI 问题重构成 UX 问题。** 自动 VAD 试图让机器猜"用户说完了吗"——这是信号处理+意图理解的复合难题，永远有边界情况。微信的设计说明：用户自己告诉你什么时候说完——这是零参数的可靠方案。算法解决不了的问题，换个交互模型可能根本不存在。

---

## 产品思维类

**Q10: 如果给你一周继续做，你会做什么？**

三个方向：1）Web Speech API 本地 TTS 作为降级方案，省掉云 TTS 成本；2）语音打断（barge-in）——用户说话时中断 TTS 播放，这是语音助手的标配体验；3）用 Web Worker 跑 VAD，避免主线程阻塞影响 UI。

---

## 像素宠物模块（可能追问）

**Q11: 宠物分配用了什么随机算法？为什么不做得更复杂？**

`random.choice(PETS)`——Python 标准库的均匀随机，从 `["robot", "cat", "dog", "alien"]` 四个选项中等概率抽取。首次登录时触发分配，结果持久化到 SQLite，后续登录通过 `username` 查表返回同一宠物。

设计考量：

1. **均匀分布就够了。** 四种宠物没有稀有度差异，不需要加权随机。`random.choice` 基于 Mersenne Twister，对非安全场景完全够用。
2. **确定性映射是核心需求，不是随机性。** 用户关心的是"我和别人宠物不一样"和"我下次回来还是同一只"，而不是随机的统计质量。SQLite 的 `username UNIQUE` 约束保证了前者，`get_or_create_user` 的"先查后插"逻辑保证了后者。
3. **不搞偏好选择是故意的。** 如果让用户选宠物，会增加一个选择步骤——登录 → 选宠物 → 确认，三个步骤。现在是登录 → 直接进。少一步交互就少一个流失点。随机的惊喜感本身也是体验的一部分。
4. **为什么不用 UUID 哈希映射？** 哈希映射可以保证同一用户名永远得到同一宠物而无需数据库，但缺点是无法切换——一旦确定就不可更改。SQLite 方案允许未来加"换宠物"功能（UPDATE 一行即可），更灵活。

**并发安全：** `get_or_create_user` 没有显式事务，但 SQLite 的 `INSERT OR IGNORE` 模式或 UNIQUE 约束下的竞态插入只会导致一方 IntegrityError。当前用"先 SELECT 后 INSERT"的非事务模式，理论上有极小概率的同名竞态。生产环境会用 `INSERT ... ON CONFLICT DO NOTHING RETURNING` 或加 `BEGIN IMMEDIATE` 事务。Hackathon 场景下单用户概率极低，未过度设计。
