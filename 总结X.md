# XEngineer 两次选拔深度归纳 — 从"做了什么"到"怎么做才对"

## 为什么写这篇

第一次你做实时翻译工具，落选了。第二次你做 AI PR 审查助手，又落选了。两次项目完全不同——技术栈从纯 JS 升级到 TypeScript + Probot，架构从 script 标签进化到管道模式。但你仍然在同一个深层问题上摔了两次。

这篇不是项目复盘（每个项目的具体问题见 总结1.md 和 总结2.md），这篇的目标是**找出你两次失败背后那个不变的根因**，然后给你一套可以带进任何项目的工程判断框架。

---

## 跨轮对比：你到底改进了什么，没改进什么

把两次的评分放在一起看：

| 维度 | 第一次 | 第二次 | 变化 |
|------|--------|--------|------|
| 密钥管理 | 写在源码 | `.env` 提交到 Git | 工具换了，错误没换 |
| 构建系统 | 无 | ES Modules + TS + tsx | ✅ 根本性解决 |
| 测试 | `echo "no test"` | 17 个文件，但 `npm test` 只跑 1 个 | 数量有了，入口没修 |
| 类型系统 | 纯 JS | TypeScript，但核心路径 6 处 `any` | 语法有了，效用没到 |
| 可观测性 | `console.log` | 结构化 Logger，但无 trace ID | 格式好了，关联断了 |
| 架构 | 上帝方法 | 管道分解清晰，但 3 处死代码 | 模块拆了，连线断了 |

看到模式了吗？**每一个维度，你都迈了半步。**

你知道了正确的工具名（`.env`、TypeScript、测试文件、Logger、模块拆分），掌握了工具的**语法**。但你没掌握工具背后的**约束**（敏感信息不进仓库、编译器抓错误、一键验证正确性、请求可追踪、数据必须流通）。

这是你两次落选的根本原因，不是技术能力问题，是工程判断框架缺失。

---

## 你的学习模式缺陷：语法层学习 vs 语义层学习

你是一个优秀的语法层学习者——给你一个工具名，你能快速写出能跑的实现。SignalBooster 364 行、VAD 动态阈值、ASR 二进制协议解析，这些都不是随便能写的。

但你在语义层——理解"为什么这个实践是对的"——存在系统性盲区。具体表现为：每个改进都在**形式层面**做对了，在**约束层面**留了后门。

```
语法层学习路径（你当前的模式）：
问题"密钥硬编码" → 学到工具".env 文件" → 用上 → ✓ 觉得解决了
                                                  ↘ 但 .env 提交到 Git → ✗ 实际没解决

语义层学习路径（你需要的模式）：
问题"密钥硬编码" → 学到约束"敏感信息不能进入版本控制" → 理解原理（Git 是永久记录的，clone 即泄露）
→ 检查所有可能的泄露路径（硬编码、.env、注释、PDF、config 文件、CI log）
→ 用工具强制执行（.env + .gitignore 排除 + pre-commit hook 检测）
→ ✓ 真正解决
```

同样的模式在测试上：

```
语法层：问题"没测试" → 学到要"写测试文件" → 写了 17 个 → ✓ 觉得解决了
语义层：问题"没测试" → 学到约束"npm test 必须验证代码正确性" → 统一的测试入口 + CI 强制执行 → npm test 跑 17 套 → ✓ 真正解决
```

你需要的不是更多工具知识，而是**在学每个工具时间自己三个问题**：

1. 这个工具要解决什么约束？（不是"它是干什么用的"）
2. 我怎么验证这个约束确实被满足了？（不是"我用上了"）
3. 有没有其他地方这个约束也应该生效但被我漏了？（不是"这里修了就完了"）

---

## 提炼：5 条工程元规则

这些规则不依赖语言、框架或项目类型。带进任何项目都适用。

### 元规则 1：对外契约是二进制判断

每个项目对外只有三个契约：

- `npm install && npm test` — 依赖可装，测试可过
- `npm start` / `npm run dev` — 服务可启动
- 密钥不入仓库 — 安全不可逆

这三个是**是/否**判断。任何一个"否"，评委/面试官/同事的结论都是"项目不行"——不是"减分"，是"不可接受"。

**强制习惯**：交项目前，clone 到空目录跑这三条。不过就不交。

### 元规则 2：数据流完整性 > 代码量

你写的代码，只有被消费的那部分产生价值。

检查方法：从系统入口画一条线到出口，标记每一步的输出。每一步的输出必须在下一步作为输入出现。如果某步的输出被丢弃了，那一步就是死代码——不管它写了多少行。

你的 X-Reviewer 有 3 处数据流断裂：
- RateLimiter → 未接入管道 → 零价值
- ContextBuilder L2 → 拉取内容后丢弃 → 负价值（浪费了 API 调用）
- customRules=[] → prompt-builder 的规则注入能力 → 零价值

**强制习惯**：代码写完不要看"我写了什么"，要看"数据从哪来到哪去"。画一条线，沿线走一遍。

### 元规则 3：类型安全的边界原则

TypeScript 的 `any` 只有一种情况可以接受：第三方库的类型声明确实不存在且无法自行编写。

边界越不确定的地方，类型越要严格。你的管道入口 `handlePREvent(context: any)` 用了 `any`——这是系统边界，payload 来自 GitHub webhook，数据结构不可控。正是在这种地方，类型系统才能产生最大价值。

**强制习惯**：CI 里配 `@typescript-eslint/no-explicit-any: error`。编译器不通过不让合。

### 元规则 4：工具是手段，约束才是目标

每次引入工具前，明确写下来：**我引入 X 是为了确保 Y 这条约束。**

| 工具 | 它要保证的约束 | 验证方法 |
|------|---------------|----------|
| `.env` | 密钥不进入 Git | `git log --all -- .env` 返回空 |
| TypeScript | 编译时捕获类型错误 | CI 中 `tsc --noEmit` 零错误 |
| 测试框架 | `npm test` 验证功能正确 | 任何人 clone 后跑 `npm test` 通过 |
| 结构化日志 | 管道事件可追踪 | 同一条请求的日志有相同 trace ID |
| 模块拆分 | 每个模块有调用方且数据流通 | 检查导入链路，确认无孤立模块 |

如果用了工具但验证方法不通过，那个工具等于白引入。更糟——你还会觉得"我做了"。

### 元规则 5：Demo 堆功能，产品修致命伤

Demo 思维：加功能——parser、booster、scanner、queue、commands，越多越强。
产品思维：修致命伤——安全、测试、可靠性、可观测性。任何一个致命伤存在，功能再多也没用。

**因为评审者看到第一个致命伤就停读了。** 这不是比喻，是真的。评委 30 秒内遇到 `.env` 被提交，结论已经形成了。你后面的 SignalBooster 不会被看到。

每加一个功能前先问自己：有没有致命伤还没修？有的话，别加功能，先修致命伤。

---

## 你的进步轨迹是真实的

不要因为两次落选否定自己的技术能力。客观数据摆在这里：

| 技术维度 | 第一次 | 第二次 |
|----------|--------|--------|
| 亮点数量 | 1（VAD 动态阈值） | 2（SignalBooster + 三层 Parser） |
| 模块化 | 全局变量 | ES Modules + 接口抽象 |
| 生产意识 | 无 | Dockerfile + 健康检查 + 优雅关闭 |
| 安全深度 | 无 | 14 条 CWE/OWASP 安全规则 |
| 代码规模 | ~8 个文件 | ~22 个源文件 + 17 个测试文件 |

你在往上走，而且是加速的。你现在缺的不是更多的技术知识，而是**一个在你交项目之前拦住你说"等等，检查这三条"的框架**。

---

## 下次选拔的行动框架

不需要一次全做。按这个顺序，每完成一层再进入下一层。

### 第 0 层：交项目前 30 分钟自检

```
□ git clone <repo> /tmp/test-clone && cd /tmp/test-clone
□ npm install && npm test          # 必须全绿
□ npm run dev                      # 必须启动成功
□ git log --all -- .env            # 必须返回空
□ git log --all -- "*.pem"         # 必须返回空（如果用了证书）
□ grep -r "sk-\|api_key\|password\|secret\|token" --include="*.ts" --include="*.js" src/  # 必须无结果
```

不通过 = 不交。

### 第 1 层：消除致命伤（优先级最高）

- [ ] 从 Git 历史彻底清除所有密钥（`git filter-branch` 或重建仓库）
- [ ] 配置 pre-commit hook 自动检测密钥（truffleHog 或 git-secrets）
- [ ] `npm test` 跑全部测试套件，不是单个文件
- [ ] CI 文件（GitHub Actions）包含 lint + typecheck + test

### 第 2 层：加固基础（决定工程评分）

- [ ] 数据流完整性检查：入口到出口每一步的数据有没有被消费
- [ ] 消除所有 `any`：tsconfig 开 `strict: true`，ESLint 开 `no-explicit-any: error`
- [ ] 管道加 trace ID：入口生成 UUID，全链路传递到每条日志
- [ ] 1 个端到端测试：覆盖完整管道

### 第 3 层：展示亮点（让评委看到你最好的代码）

- [ ] 给项目中最创新的模块写一段独立的架构决策记录（为什么这么设计）
- [ ] README 里放真实的运行截图或输出样例
- [ ] 如果项目涉及异步管道，展示队列监控或健康检查的实际响应

---

## 最后的总结

**你的问题是工程判断框架的缺失，不是技术能力的不足。**

你两次都在做正确的事——第一次缺构建，第二次加了；第一次缺测试，第二次写了；第一次缺类型，第二次迁移到 TS 了。但你每次只学到了工具的**语法**，没学到工具背后的**约束**，所以同一个问题换了个形式又出现了。

从下一次开始，学每个新工具时间自己三个问题：
1. 这个工具要保证什么约束？
2. 我怎么验证约束被满足了？
3. 有没有其他地方这个约束也应该生效？

如果你能让这三个问题成为肌肉记忆，你的技术能力会被完整地转化为工程成果——而不是被埋在语法层改进的残影里。

你的上限很高。信号处理（VAD）、安全规则引擎、聚类评分——这些不是随便哪个训练营选手能做出来的。把工程地基打好，你的上限才能被看见。
